[
  {
    "name": "View Progress empty state",
    "steps": [
      {
        "send": { "callback": "progress:view" },
        "expect": [
          { "method": "sendMessage", "payload": { "text": "No cards yet — create a deck and add some words to start tracking progress!" } }
        ]
      }
    ]
  }
]
