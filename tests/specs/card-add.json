[
  {
    "name": "Add Card empty state",
    "steps": [
      {
        "send": { "callback": "card:add" },
        "expect": [
          { "method": "sendMessage", "payload": { "text": "You need a deck first — create one before adding cards." } }
        ]
      }
    ]
  }
]
