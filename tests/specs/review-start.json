[
  {
    "name": "Start Review empty state",
    "steps": [
      {
        "send": { "callback": "review:start" },
        "expect": [
          { "method": "sendMessage", "payload": { "text": "No decks yet — create one to start reviewing!" } }
        ]
      }
    ]
  }
]
